{
  "name": "r1-py",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run pywrangler sync && npx wrangler dev",
    "deploy": "uv run pywrangler sync && npx wrangler deploy",
    "build": "uv run pywrangler sync",
    "start": "uv run pywrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.58.0"
  }
}