[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "xxhash"
version = "3.5.0"
description = "Stub xxhash for Pyodide compatibility"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["src/xxhash"]
